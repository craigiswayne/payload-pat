{
  "name": "bait",
  "version": "1.0.0",
  "description": "Chrome Extension that allows you to listen for specific HTML requests and then act upon the response",
  "main": "index.js",
  "scripts": {
    "sass": "node-sass ./assets/styles -o dist",
    "watch-styles": "npm run sass && node-sass --watch ./assets/styles -o dist",
    "ts": "tsc -p ./",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "chrome",
    "extension",
    "network",
    "payload"
  ],
  "author": "Craig Wayne",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^4.3.1",
    "font-awesome": "^4.7.0",
    "node-sass": "^4.12.0",
    "typescript": "^3.5.3",
    "vue": "^2.6.10"
  },
  "devDependencies": {
    "concat": "^1.0.3"
  }
}
